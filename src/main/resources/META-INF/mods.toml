modLoader="javafml" #mandatory
loaderVersion="[35,)" #mandatory This is typically bumped every Minecraft version by Forge. See our download page for lists of versions.
license="All rights reserved"
#issueTrackerURL="http://my.issue.tracker/" #optional
[[mods]] #mandatory
modId="destinymod" #mandatory
version="${file.jarVersion}" #mandatory
displayName="Destiny MC" #mandatory
#updateJSONURL="http://my.issue.tracker/" #optional
#displayURL="http://my.issue.tracker/" #optional
logoFile="examplemod.png" #optional
credits="All inspiration comes from the great work done at Bungie for Destiny 2" #optional
authors="Adi Poluri and Jayden Cang" #optional
description='''
A mod that allows u to harness the powers of the guardian.
'''
[[dependencies.destinymod]] #optional
    # the modid of the dependency
    modId="forge" #mandatory
    # Does this dependency have to exist - if not, ordering below must be specified
    mandatory=true #mandatory
    # The version range of the dependency
    versionRange="[35,)" #mandatory
    # An ordering relationship for the dependency - BEFORE or AFTER required if the relationship is not mandatory
    ordering="NONE"
    # Side this dependency is applied on - BOTH, CLIENT or SERVER
    side="BOTH"
# Here's another dependency
[[dependencies.destinymod]]
    modId="minecraft"
    mandatory=true
# This version range declares a minimum of the current minecraft version up to but not including the next major version
    versionRange="[1.16.4,1.17)"
    ordering="NONE"
    side="BOTH"
